<div class="code-posts">
  <div class="col-md-10"></div>
  <% if current_user %>
      <div class="col-md-2 text-right new_button padding6">
        <%= link_to raw(%Q(<i class="glyphicon glyphicon-pencil"> </i>#{t(:new)})), new_blog_path, class: "btn btn-primary" %>
      </div>
  <% end %>
</div>

<div class="code-posts">
  <%= render partial: 'blogs', locals: {blogs: @blogs} %>
</div>