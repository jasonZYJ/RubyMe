<div class="pagehead panel panel-default">
  <div class="container">
      <div class="col-md-10">
        <h4><%= "#{Settings.site.name_zh} · #{site_info}" %></h4>
      </div>
      <% if current_user %>
      <div class="col-md-2">
        <%= link_to raw('<i class="glyphicon glyphicon-pencil"> </i>发布新帖子'), new_post_path, class: "new_button btn btn-primary" %>
      </div>
      <% end %>
    </div>
</div>


